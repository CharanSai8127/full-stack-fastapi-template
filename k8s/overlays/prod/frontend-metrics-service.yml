apiVersion: v1
kind: Service
metadata:
  name: frontend-metrics
  namespace: prod
  labels:
    app: frontend
    metrics: enabled
spec:
  selector:
    app: frontend
  ports:
    - name: metrics
      port: 8081     # adjust to your frontend’s metrics port
      targetPort: 8081

